const name="shan"
const age = 20
console.log(`my name is ${name} and also my age is ${age}`);

const newGame= new String('pubg')
console.log(newGame);
console.log(newGame[0]);  // in '0' position is 'p'
console.log(newGame.length); // to check the leanth of the string
console.log(newGame.toUpperCase()); // use to make all words capital
console.log(newGame.charAt(3));  // chek the position of the words 

const newString="  shan  "
console.log(newString)
console.log(newString.trim())  // trim is use to remove the spaces

const url="https://shan.com/shan24%sanga"
console.log(url.replace('%20','-'))  // use to replace the words to anything
console.log(url.includes('shan')) //chek that words avilable or not 